# ============================================================
# requirements.txt — Eco-Smart Classifier (Production)
# ============================================================
# Only runtime dependencies needed for serving predictions.

# Core ML
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# NLP (gensim only needed if NLP vectorizer is Word2Vec/FastText)
gensim>=4.3

# API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
prometheus-fastapi-instrumentator>=6.1
aiofiles>=23.0

# PDF export
fpdf2>=2.7

# MLflow (for model registry loading — optional fallback to pkl)
mlflow>=2.10
